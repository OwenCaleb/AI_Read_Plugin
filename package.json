{
  "name": "ai_read_plugin",
  "version": "1.0.0",
  "description": "调用Coze工作流API的Edge浏览器扩展",
  "main": "index.js",
  "scripts": {
    "start": "node index.js",
    "build-icons": "node create_icons.js",
    "build": "mkdir -p dist && zip -r dist/coze_extension.zip manifest.json popup.html popup.js background.js icons/*.png",
    "test": "echo \"Error: no test specified\" && exit 1"
  },
  "keywords": ["edge-extension", "browser-extension", "coze", "api"],
  "author": "",
  "license": "ISC",
  "dependencies": {
    "axios": "^1.8.4",
    "canvas": "^2.11.2",
    "readline-sync": "^1.4.10"
  }
}
